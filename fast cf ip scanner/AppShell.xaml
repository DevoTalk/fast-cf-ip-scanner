<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="fast_cf_ip_scanner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodel="clr-namespace:fast_cf_ip_scanner.ViewModels"
    x:DataType="viewmodel:BaseViewModel"
    xmlns:local="clr-namespace:fast_cf_ip_scanner"
    Shell.FlyoutBehavior="Disabled"
    xmlns:views="clr-namespace:fast_cf_ip_scanner.Views"
    >

    <!--<ShellContent
        
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->
    <TabBar>
        <!--<Tab
            Title="m">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
        </Tab>-->

        <Tab
            Title="Title">
            <ShellContent ContentTemplate="{DataTemplate views:ScanPage}"/>
        </Tab>

        <Tab
            Title="Setting">
            <ShellContent ContentTemplate="{DataTemplate views:SettingPage}"/>
        </Tab>
        
    </TabBar>

</Shell>
